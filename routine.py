import os

# Check where the current working directory is:
print(os.getcwd())

DIR = "" # Change this to the

execfile(DIR + "src/check_python.py", {})
pip install pandas
execfile(DIR + "src/check_pandas.py", {})
